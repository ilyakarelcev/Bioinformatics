
Когда ты впервые реально запустишь embeddings backend, могут понадобиться еще две тяжелые вещи:

1. ProtT5 model weights
2. per-protein.h5 с embeddings белков

ProtT5 веса могут скачаться через HuggingFace автоматически при первом использовании модели.

А per-protein.h5 — это не база, которую делала команда. Это файл с предрассчитанными protein embeddings от UniProt. Код ожидает его локально или пытается скачать через bootstrap, если настроен источник.



**ProtT5-модель**  
Да: локальная модель Rostlab/prot_t5_xl_uniref50 берёт аминокислотную строку и превращает её в embedding-вектор.


Модель лежит тут:
C:\Users\53k\.cache\huggingface\hub\models--Rostlab--prot_t5_xl_uniref50
эти 10.5 GB — это **не UniProt и не база белков**. Это сама **нейросеть ProtT5**, которая умеет превращать аминокислотную строку в embedding.

